# 立即执行：TradeGo 每日 SEO Master Cron

你的唯一目标: 用 write 工具生成 5 篇紧固件行业 SEO 文章到 `content/articles/*.json`, 每篇分数 >= 85。

## Step 1: 跑脚本拿选题
```bash
cd /Users/zhangming/workspace/tradego-fasteners-v2
node scripts/seo-topic-selector.js 5
```
脚本会输出 5 个 topic (含 title_en/keywords/relatedProducts 等)。

## Step 2: 对每个 topic, 调 sessions_spawn 并行生成
每个 topic 1 个子 session, 任务极简:
- 子 session 只生成 1 篇
- 必须用 write 工具写文件
- 必须返回完整文件路径

子 session 任务模板:
```
任务: 生成 1 篇 SEO 文章, 写到 /Users/zhangming/workspace/tradego-fasteners-v2/content/articles/{slug}.json

Topic 数据:
{title_en, keywords, relatedProducts}

要求 (严格执行):
1. 必读模板: /Users/zhangming/workspace/tradego-fasteners-v2/content/articles/{任意已有高分文章}.json (推荐: zimbabwe-fastener-market-guide.json)
2. 必做 web_search 2 次 (找 ISO 标准 + 行业数据)
3. 10 语言: en/zh/es/ar/fr/pt/ru/ja/de/hi 全部真实翻译 (不要占位)
4. 5-7 sections, 3-5 FAQ, 5 dataSource, E-E-A-T author 完整
5. en 1500-2500 words
6. 用 write 工具一次写入完整文件
7. 验证: bash ~/.agents/skills/seo-universal-author/scripts/validate-article.py content/articles/{slug}.json
8. 分数 >= 85 才算成功

完成 = 文件存在 + 分数 >= 85。返回完整路径。
```

## Step 3: 验证
所有子 session 完成后:
```bash
ls -la /Users/zhangming/workspace/tradego-fasteners-v2/content/articles/*.json | wc -l
```
确认新增 5 篇。

## 重要规则
- 不允许只"说"做了什么 — 必须用 write 工具真的写文件
- 完成后返回: "📊 TradeGo 文章生成: {new_count} 篇, 平均 {avg_score} 分"
- 工具: exec, write, edit, read, web_search, sessions_spawn
- 预算: 90 分钟
- 先只跑 1 篇 (gen-count=1) 验证流程, 跑通后 4 篇并行
